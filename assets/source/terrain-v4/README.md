# Connected hill family proposal

Generated concept only, not an atlas or a game screenshot. References are the approved
`base-v1/terrain-grass.png`, `terrain-hill.png`, and `terrain-mountain.png`.
The exact built-in ImageGen request is saved in `prompt.json`.
